#ifndef __DSPLUGINAPI_TYPEDEF_H__
#define __DSPLUGINAPI_TYPEDEF_H__

#include <string>

namespace DSPluginAPI
{
    // 全局定义ID
    typedef unsigned int ID;
    const ID idInValid = static_cast<unsigned int>(-1); // 无效ID

    // 全局定义Code
    typedef std::string Code;
    const Code codeInValid = ""; // 无效编号

    // 全局定义序号
    typedef unsigned int No;
    const No noInValid = static_cast<unsigned int>(-1); // 无效序号

    // 无效的时间
    const time_t tmInValid = -1;
}

#endif
